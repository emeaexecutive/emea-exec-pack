// Swap this stub with your real translation provider (OpenAI/Azure/DeepL).
export async function translateTo(
  text: string,
  target: "en" | "es" | "fr" | "de",
  source?: string
) {
  // Stub returns original text to keep core lightweight.
  return text;
}
